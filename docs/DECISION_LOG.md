# Decision Log

Append-only; newer decisions at the bottom. Never edit or delete past rows — supersede a decision with a new row that references the old one.

| # | Date | Decision | Rationale | Alternatives considered | Owner |
|---|---|---|---|---|---|
| D-001 | 2026-08-22 | GLM implements; humans and Qwen review all output | Maximizes implementation throughput while keeping human judgment on every change | Human-only coding; Qwen as implementer with GLM as reviewer | Sonolo team |
| D-002 | 2026-08-22 | Monorepo: `apps/`, `backend/`, `content/`, `docs/`, `scripts/`, `tests/` | One repo keeps control docs next to code and simplifies the task board | Separate repositories per app | Sonolo team |
| D-003 | 2026-08-22 | Mobile: React Native + Expo + TypeScript | Single TypeScript codebase across mobile and web; Expo speeds up audio/voice iteration | Flutter; separate native iOS and Android apps | Sonolo team |
| D-004 | 2026-08-22 | Web: Next.js landing page | Fast marketing site that shares TypeScript conventions with the mobile app | Plain static HTML; Astro | Sonolo team |
| D-005 | 2026-08-22 | Backend: FastAPI + Python 3.12, PostgreSQL, Redis, WebSockets | Native async and WebSocket support; Python ecosystem for open-source AI models | Node.js/NestJS; Django | Sonolo team |
| D-006 | 2026-08-22 | RevenueCat for payments, PostHog for analytics, Sentry for errors | Buy-not-build for non-core services | Custom billing; self-hosted analytics | Sonolo team |
| D-007 | 2026-06-22 | MVP is English-first; Canadian French is waitlist/Phase 2 | Maximizes launch speed, reduces content/TTS/scoring scope, and validates French demand via waitlist | Bilingual English + French MVP | Founder |
| D-008 | 2026-08-22 | Use uuid6 library for uuid7 PKs | Python 3.12 stdlib lacks uuid7; uuid6 is maintained and lightweight | Custom uuid generation, waiting for Python 3.14 stdlib | Founder |
| D-009 | 2026-08-22 | SN-014 was implemented just before its SN-014A prerequisite arrived; both were stabilized in place rather than reverting | Both cards' requirements verified together (117→119 passing tests); reverting verified work added risk without adding safety | Revert SN-014 and re-apply after SN-014A | GLM 5.3 + Sonolo team |
| D-010 | 2026-08-22 | `vocabulary_cards` stays user-scoped; the SN-009 pack is shared content lazily materialized per user on first `/api/review/due` (deterministic per-user UUIDs, FSRS priors from the pack); `user_vocabulary_states` table not created | SN-006 schema and SN-012 endpoints were already user-scoped; a separate state table would duplicate FSRS scheduling state | SN-014A option of a `user_vocabulary_states` table; globally shared scheduling | GLM 5.3 + Sonolo team |
| D-011 | 2026-08-22 | WebSocket auth trusts the verified JWT `sub` claim without a per-socket DB lookup; revocation latency equals token lifetime (60 min default) | Avoids holding a DB session per socket and event-loop binding issues; tokens are issued only at login | Per-connection user lookup; short-lived WS-only tokens; server-side revocation list | GLM 5.3 + Sonolo team |
| D-012 | 2026-09-02 | Mastery is unavailable when any required component (exercise scores, unit-test skill scores, EMA) is missing. No numeric fallback. Advancement does not occur while mastery is unavailable. | Part V §5.2 does not define missing-data behavior; treating missing as 0 or omitting a weight would silently change the 0.40/0.35/0.25 formula | Treat missing as 0; drop the missing term and renormalize; invent an initial EMA | Founder/Architect |
| D-013 | 2026-09-02 | Band completion (Part V §5.5, including the applicable band test) and certificate eligibility (Part IV §4.4, no band test) are separate. C2 evaluates both and does not issue certificates or auto-pass band tests. | Part IV §4.4 and Part V §5.3/§5.5 disagree on whether a band test is required; merging them would invent a product rule | Require band test for certificates; drop band test from band completion | Founder/Architect |
| D-022 | 2026-09-02 | First valid session score initializes persisted EMA to that score; later updates use C2 `update_ema`. C2 still raises if EMA is missing. | Part V does not define the first EMA; D-012 forbids a C2 fallback. Persisting the first session score is an interpretation, not a formula change. | Leave EMA null forever; invent 50; skip the 0.25 term | Founder acceleration default |
| D-023 | 2026-09-02 | Every ReadingExercise uses GET → POST /start → POST /submit, including L1–L3 (no deadline). Retry = new /start. No pause/resume/grace. | One client lifecycle; D-020 server clock | Start-on-GET; L1–L3 skip /start | Founder |
| D-024 | 2026-09-02 | Practice evidence is `skill_exercise_attempts`; unit-test per-skill is `unit_test_skill_evidence`; EMA is `user_skill_levels.ema_score`. `learning_sessions` is session envelope only. | D-018 forbids overloading `learning_sessions` as last-20 | One JSON ledger; query last-20 from learning_sessions | Founder/Architect |
| D-025 | 2026-09-02 | L1–L3 short_answer: normalize case/whitespace/boundary punctuation; exact `correct_answer` = 100; all authored `keywords` present = 100; `accepted_answers` full-string = 80. L4+ short_answer not scored. | Smallest deterministic reading of §11.2 + D-015/D-016 | Fuzzy match; LLM; any-keyword | Founder acceleration default |
| D-026 | 2026-09-02 | Vocabulary Hunt catalog is file-backed (manifest type `vocabulary_hunts`), not stored in `reading_exercises.questions`. Attempts use the shared evidence table with `activity_type=vocabulary_hunt`. | D-014; listening already file-backed | New hunt catalog table | Implementation detail |
| D-027 | 2026-09-02 | F3 `writing_complete` is true when every `writing_ids` entry has an accepted submit. Max 3 writing submissions per exercise (first counts as revision 1). Highest writing score per content_id is the last-20 value. Error-fix raw may exceed 100 then clamp to [0,100] for evidence. Stage 1 spelling skipped: no dictionary library in stack. | Part XV writing block lists 5a–5c; Part X §10.5; C4 mastery clamp | Separate writing_required_activities field; 1 initial + 3 revisions | Implementation default |
| D-028 | 2026-09-02 | Dictation word match: casefold, boundary punctuation stripped, whitespace collapsed (same as reading normalize). Positional word alignment. Exact token match, or Levenshtein ≤ 1 when both tokens have length ≥ 4. Short words (≤3) require exact match. Empty expected segment scores 0. Gym MC evaluate without `dictation` keeps SN-050 scoring. Transcript of dictation lines is omitted on GET and returned only on evaluate. Full-replay count is accepted and ignored for score (max 3 is client policy metadata). | Part XI §11.1 fuzzy dictation; C5 additive compatibility | Semantic synonym credit; hide gym turn text | Implementation default |
| D-029 | 2026-09-02 | Daily Mix v2 is GET `/api/learn/daily-mix`. Deterministic per user+local date. Does not persist mix rows (no C0 change). Does not award XP; `xp_possible` is display-only (15/skill item + 15 if vocab due). Legacy goals `travel`/`love` map to `casual` weights. Returning after 30 days sets `welcome_back` and prefers easier content; streak/XP logic is untouched. Daily quests remain the SN-014 idempotent XP path. | Part XII; C6 forbidden XP/idempotency changes | Store mix in DB; change quest rewards | Implementation default |
| D-030 | 2026-09-02 | Diagnostic state lives in `learning_sessions` with skill=`diagnostic` (no new table). Items are fixed MC (semi-adaptive: not CAT). Diagnostic answers are not last-20 evidence. Completion writes `user_skill_levels` and `users.sonolo_level=min(skills)` only; does not set `onboarding_completed`, EMA, or XP. §6.5 correction is one-time within 7 days of placement, using real activity scores. Restart after complete returns the stored placement. | Part VI; C7 forbidden second mastery engine | New diagnostic table; LLM speaking diagnostic | Implementation default |
| D-031 | 2026-09-02 | C8 Journey Map lock/current/complete uses `user_unit_progress.unit_test_passed` on the sequential F1→A6 graph (F1 has no prereq). Band complete/unlock on the map = all 6 units in the band passed their unit test. Not C2 `check_band_completion` (band test + skill levels). Not `users.current_level` or `users.sonolo_level`. GET `/api/learn/journey` is the C8 additive endpoint. Certificate/cutscene copy omitted (not in C8). | C8 chunk lists new APIs; §14.3 visual is unit-progress based | Use C2 band completion for map unlock; wait for C10 to add an API | Implementation default |
| D-032 | 2026-09-02 | C9 GET `/api/progress/skills` reads `user_skill_levels` and calls C2 `display_level`, `readiness_level`, and `get_skill_recommendation`. Missing skill rows default to 1 (C0 default; same as C6) without writing. Does not use `users.current_level`, XP level, or speaking sub-dimensions. Four-skill radar uses existing `react-native-svg` (already on Progress). Alert copy is the C2 message; UI does not recompute gap. | C9 “add skill level API call”; implementation plan `/api/progress/skills` | Put levels on `/users/me`; reimplement §5.6 in the client | Implementation default |
| D-033 | 2026-09-02 | C10 Unit Detail uses GET `/learn/units/{code}` (C1 catalog) + GET `/learn/journey` (C0 skill flags). Intra-unit skill blocks are independently available. Unit Test unlocks only when speaking+listening+reading+writing flags are complete — not from content IDs. Vocab/grammar/review have no C0 completion flags so they are not Unit Test gates. Listening navigates to existing `/listening/{id}`; reading/writing/grammar/vocab/review/unit-test have no activity screens yet so taps do not invent routes. | C10 Unit Detail UI; existing C1/C8 APIs | New `/learn/units/{code}/progress`; stub activity screens | Implementation default |
| D-034 | 2026-09-02 | C11 Golden Slice cannot complete F3 end-to-end on C0–C10. Executable: F3 listening/reading/hunt/writing APIs write evidence and L/R/W complete flags. Blocked: empty `vocab_primer_ids`/`speaking_ids`/`grammar_spotlight_id`; no `test-F3` content or unit-test API; `speaking_complete` and `unit_test_passed` have no production writer; C2 mastery stays unavailable without unit-test skill evidence; `orchestrate_after_practice` passes `unit_tests=[]`; F3 stays journey-locked until F1/F2 unit tests (also missing). C11 tests document the executable path and these blockers. No production code. | C11 is integration-only | Build unit-test/speaking/vocab/grammar engines inside C11 | Founder/Architect |
| D-035 | 2026-09-02 | C12 Unit Test engine is GET/POST `/api/learn/unit-test/{unit_code}`. File-backed `test-F3` (manifest type `unit_tests`). Listening/reading reuse C5/C3 scorers; writing articles+keywords are deterministic (no LLM); speaking uses existing SN-011 `SessionEvaluator` (no new rubric). Pass uses C2 `UNIT_TEST_OVERALL_MIN`/`UNIT_TEST_SKILL_MIN` with no pre-compare rounding. 24h fail cooldown from last `unit_test_skill_evidence.submitted_at` (no schema change). POST requires four `user_unit_progress` skill flags (C10). Does not reimplement C8 F1/F2 graph. `record_unit_test_sitting` is the production `unit_test_passed` writer. `orchestrate_after_unit_test` / `orchestrate_after_practice` pass real `UnitTestEvidence` into C2. | Part XI §11.3; F3 §15.3; C11 D-034 | LLM writing for unit test; new fail-timestamp column; F1/F2 content in C12 | Implementation default |
| D-036 | 2026-09-02 | C13 F3 SpeakUp is new published content (clerk + SpeakSprint scenarios, pronunciation drill) plus a production `speaking_complete` writer. Completion is true iff every F3 `speaking_ids` entry has a submitted `skill_exercise_attempts` row (same C3–C5 pattern). Session-backed activities count only when SN-014 XP-eligible (user turn + ≥15s). Pronunciation evaluate on the bound F3 drill counts. No new C0 column. Gym SN-011 sessions and Pronunciation Lab drills without `unit_id` do not write speaking evidence or completion. Existing `SessionEvaluator` is reused; F3 vocab/grammar are passed as `scenario_targets`. Gym catalog excludes unit-bound scenarios/drills so the 110/12 rails stay intact. | C13; existing SN-011/SN-014/SN-049; C3–C5 completion | New speaking engine; complete on any session; new schema column | Implementation default |
| D-037 | 2026-09-02 | C14 F3 Vocab Primer, Grammar Spotlight, and Review & Reinforce are catalog/content activities, not C0 progression flags. Clarifies D-033: still no `vocab_complete` / `grammar_complete` / `review_complete`; they are not Unit Test gates (C10/C12 remain four-skill flags). Primer is the 20 authoritative F3 words in the existing vocabulary pack + FSRS materialization (`vocab_primer_ids`). Grammar Spotlight is file-backed (`grammar` manifest type, no mastery engine, no LLM). F3 Review is GET `/api/learn/review/{unit}` — a unit-bound projection over existing FSRS cards for those primer ids. Generic `/api/review/due` and `/api/review/answer` are unchanged. | C14; D-033; SN-012 FSRS | New completion columns; second FSRS; grammar engine | Implementation default |
| D-038 | 2026-09-02 | C15 F1/F2 are the Learning Architecture §3.1 units (Arrival Day / Finding Home), Level 1, `en-CA`. Each has 15 vocab targets (contract minimum), three short listenings so F1+F2 can supply C2's 6 L1 exercises without fake rows, two readings + one hunt, three writings, three speaking activities, grammar spotlight, and a C12-schema unit test with new items (not F3 copies). C8 graph and C2 thresholds are unchanged. Vocab/grammar/review remain non-gates (D-037). | C15; §3.1 table; C2 L1→L2 | Bypass C8; fewer than 6 listenings; copy F3 tests | Implementation default |
